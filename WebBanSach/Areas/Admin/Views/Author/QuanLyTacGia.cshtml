@model IEnumerable<Media.Models.TacGia>
@{
    ViewData["Title"] = "Quản lý Tác giả";
    Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml";
}

<div class="min-h-screen bg-gradient-to-br from-gray-50/50 via-blue-50/30 to-purple-50/30 dark:from-gray-900 dark:via-blue-900/10 dark:to-purple-900/10 py-8">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        <!-- Header -->
        <div class="mb-8">
            <div class="flex flex-col sm:flex-row justify-between items-start sm:items-center gap-6">
                <div class="flex items-center gap-4">
                    <div class="p-4 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl shadow-2xl">
                        <i class='bx bx-user-circle text-2xl text-white'></i>
                    </div>
                    <div>
                        <h1 class="text-4xl font-bold text-gray-900 dark:text-white bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
                            Quản Lý Tác Giả
                        </h1>
                        <p class="text-gray-600 dark:text-gray-400 mt-2 text-lg">Quản lý và theo dõi toàn bộ tác giả trong hệ thống</p>
                    </div>
                </div>
                <div class="flex gap-3">
                    <a asp-action="ThemTacGia" class="group inline-flex items-center px-6 py-3 bg-gradient-to-r from-green-500 to-emerald-600 hover:from-green-600 hover:to-emerald-700 text-white font-semibold rounded-2xl transition-all duration-300 hover:shadow-2xl hover:scale-105 transform hover:-translate-y-1">
                        <i class='bx bx-plus mr-2 group-hover:scale-110 transition-transform'></i>
                        Thêm Tác Giả
                    </a>
                </div>
            </div>
        </div>

        <!-- Form tìm kiếm -->
        <div class="bg-gradient-to-br from-white to-purple-50 dark:from-gray-800 dark:to-purple-900/20 rounded-3xl shadow-xl border border-purple-100/50 dark:border-purple-800/30 p-6 mb-6">
            <div class="flex items-center gap-3 mb-4">
                <div class="p-3 bg-gradient-to-br from-purple-500 to-purple-600 rounded-2xl">
                    <i class='bx bx-search text-xl text-white'></i>
                </div>
                <h2 class="text-2xl font-bold text-gray-900 dark:text-white">Tìm kiếm Tác giả</h2>
            </div>
            <form method="get" asp-action="QuanLyTacGia">
                <div class="flex flex-col lg:flex-row gap-4 items-center justify-between">
                    <div class="flex flex-col sm:flex-row gap-4 flex-1 w-full">
                        <div class="relative flex-1 max-w-2xl">
                            <i class='bx bx-search absolute left-4 top-1/2 transform -translate-y-1/2 text-gray-400 text-xl'></i>
                            <input type="text"
                                   name="searchString"
                                   value="@ViewData["CurrentFilter"]"
                                   class="w-full pl-12 pr-4 py-4 bg-white dark:bg-gray-800 border-2 border-gray-200 dark:border-gray-700 rounded-2xl text-gray-900 dark:text-white placeholder-gray-500 dark:placeholder-gray-400 focus:border-blue-500 dark:focus:border-blue-400 focus:ring-4 focus:ring-blue-100 dark:focus:ring-blue-900/30 transition-all duration-300"
                                   placeholder="Tìm kiếm theo tên, quốc tịch hoặc tiểu sử..." />
                        </div>
                    </div>
                    <div class="flex gap-3">
                        <button type="submit" class="px-6 py-4 bg-gradient-to-r from-blue-500 to-blue-600 hover:from-blue-600 hover:to-blue-700 text-white font-semibold rounded-2xl transition-all duration-300 hover:shadow-lg hover:scale-105 flex items-center">
                            <i class='bx bx-search mr-2'></i>
                            Tìm kiếm
                        </button>
                        <a asp-action="QuanLyTacGia" class="px-6 py-4 bg-gradient-to-r from-gray-500 to-gray-600 hover:from-gray-600 hover:to-gray-700 text-white font-semibold rounded-2xl transition-all duration-300 hover:shadow-lg hover:scale-105 flex items-center">
                            <i class='bx bx-reset mr-2'></i>
                            Đặt lại
                        </a>
                    </div>
                </div>
            </form>
        </div>

        <!-- Thống kê -->
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6 mb-8">
            <!-- Total Authors -->
            <div class="group bg-gradient-to-br from-white to-blue-50 dark:from-gray-800 dark:to-blue-900/20 rounded-3xl shadow-xl border border-blue-100/50 dark:border-blue-800/30 p-6 hover:shadow-2xl transition-all duration-300 hover:scale-105 transform hover:-translate-y-1 cursor-pointer">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 dark:text-gray-400 uppercase tracking-wide">Tổng số tác giả</p>
                        <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">@Model.Count()</p>
                        <div class="flex items-center mt-2">
                            <i class='bx bx-user text-blue-500 mr-1'></i>
                            <span class="text-sm text-blue-600 dark:text-blue-400 font-medium">Tác giả hệ thống</span>
                        </div>
                    </div>
                    <div class="p-3 bg-gradient-to-br from-blue-500 to-blue-600 rounded-2xl group-hover:scale-110 transition-transform duration-300">
                        <i class='bx bx-user-circle text-2xl text-white'></i>
                    </div>
                </div>
            </div>

            <!-- Vietnamese Authors -->
            <div class="group bg-gradient-to-br from-white to-green-50 dark:from-gray-800 dark:to-green-900/20 rounded-3xl shadow-xl border border-green-100/50 dark:border-green-800/30 p-6 hover:shadow-2xl transition-all duration-300 hover:scale-105 transform hover:-translate-y-1 cursor-pointer">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 dark:text-gray-400 uppercase tracking-wide">Tác giả Việt Nam</p>
                        <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">@Model.Count(t => !string.IsNullOrEmpty(t.QuocTich) && t.QuocTich.Contains("Việt Nam"))</p>
                        <div class="flex items-center mt-2">
                            <i class='bx bx-flag text-green-500 mr-1'></i>
                            <span class="text-sm text-green-600 dark:text-green-400 font-medium">Trong nước</span>
                        </div>
                    </div>
                    <div class="p-3 bg-gradient-to-br from-green-500 to-green-600 rounded-2xl group-hover:scale-110 transition-transform duration-300">
                        <i class='bx bx-flag text-2xl text-white'></i>
                    </div>
                </div>
            </div>

            <!-- Foreign Authors -->
            <div class="group bg-gradient-to-br from-white to-amber-50 dark:from-gray-800 dark:to-amber-900/20 rounded-3xl shadow-xl border border-amber-100/50 dark:border-amber-800/30 p-6 hover:shadow-2xl transition-all duration-300 hover:scale-105 transform hover:-translate-y-1 cursor-pointer">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 dark:text-gray-400 uppercase tracking-wide">Tác giả nước ngoài</p>
                        <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">@Model.Count(t => !string.IsNullOrEmpty(t.QuocTich) && !t.QuocTich.Contains("Việt Nam"))</p>
                        <div class="flex items-center mt-2">
                            <i class='bx bx-globe text-amber-500 mr-1'></i>
                            <span class="text-sm text-amber-600 dark:text-amber-400 font-medium">Quốc tế</span>
                        </div>
                    </div>
                    <div class="p-3 bg-gradient-to-br from-amber-500 to-amber-600 rounded-2xl group-hover:scale-110 transition-transform duration-300">
                        <i class='bx bx-globe text-2xl text-white'></i>
                    </div>
                </div>
            </div>

            <!-- No Biography -->
            <div class="group bg-gradient-to-br from-white to-red-50 dark:from-gray-800 dark:to-red-900/20 rounded-3xl shadow-xl border border-red-100/50 dark:border-red-800/30 p-6 hover:shadow-2xl transition-all duration-300 hover:scale-105 transform hover:-translate-y-1 cursor-pointer">
                <div class="flex items-center justify-between">
                    <div>
                        <p class="text-sm font-semibold text-gray-600 dark:text-gray-400 uppercase tracking-wide">Chưa có tiểu sử</p>
                        <p class="text-3xl font-bold text-gray-900 dark:text-white mt-2">@Model.Count(t => string.IsNullOrEmpty(t.TieuSu))</p>
                        <div class="flex items-center mt-2">
                            <i class='bx bx-file text-red-500 mr-1'></i>
                            <span class="text-sm text-red-600 dark:text-red-400 font-medium">Cần cập nhật</span>
                        </div>
                    </div>
                    <div class="p-3 bg-gradient-to-br from-red-500 to-red-600 rounded-2xl group-hover:scale-110 transition-transform duration-300">
                        <i class='bx bx-file text-2xl text-white'></i>
                    </div>
                </div>
            </div>
        </div>

        <!-- Main Card -->
        <div class="bg-gradient-to-br from-white to-blue-50 dark:from-gray-800 dark:to-blue-900/20 rounded-3xl shadow-2xl border border-blue-100/50 dark:border-blue-800/30 overflow-hidden">
            <!-- Card Header -->
            <div class="px-8 py-6 bg-gradient-to-r from-blue-600 via-purple-600 to-indigo-600">
                <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between">
                    <div class="flex items-center gap-4">
                        <div class="p-3 bg-white/20 rounded-2xl backdrop-blur-sm border border-white/30">
                            <i class='bx bx-list-ul text-2xl text-white'></i>
                        </div>
                        <div>
                            <h2 class="text-2xl font-bold text-white">Danh Sách Tác Giả</h2>
                            <p class="text-blue-100 text-sm mt-1">@Model.Count() tác giả được tìm thấy trong hệ thống</p>
                        </div>
                    </div>
                    <div class="mt-4 sm:mt-0 flex items-center gap-3">
                        <span class="bg-white/20 text-white text-sm px-4 py-2 rounded-full backdrop-blur-sm border border-white/30">
                            <i class='bx bx-sort-alt-2 mr-2'></i>
                            Sắp xếp
                        </span>
                    </div>
                </div>
            </div>

            <!-- Table -->
            <div class="overflow-x-auto">
                <table class="w-full">
                    <thead>
                        <tr class="bg-gradient-to-r from-gray-50 to-blue-50 dark:from-gray-800 dark:to-blue-900/20 border-b-2 border-gray-200/50 dark:border-gray-700/50">
                            <th class="px-8 py-6 text-left text-sm font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider">
                                <div class="flex items-center gap-2">
                                    <i class='bx bx-user'></i>
                                    Tên tác giả
                                </div>
                            </th>
                            <th class="px-8 py-6 text-left text-sm font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider">
                                <div class="flex items-center gap-2">
                                    <i class='bx bx-flag'></i>
                                    Quốc tịch
                                </div>
                            </th>
                            <th class="px-8 py-6 text-left text-sm font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider">
                                <div class="flex items-center gap-2">
                                    <i class='bx bx-file'></i>
                                    Tiểu sử
                                </div>
                            </th>
                            <th class="px-8 py-6 text-left text-sm font-bold text-gray-700 dark:text-gray-300 uppercase tracking-wider">
                                <div class="flex items-center gap-2">
                                    <i class='bx bx-cog'></i>
                                    Thao tác
                                </div>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-200/30 dark:divide-gray-700/30">
                        @foreach (var item in Model)
                        {
                            <tr class="group hover:bg-gradient-to-r hover:from-blue-50/50 hover:to-purple-50/50 dark:hover:from-blue-900/10 dark:hover:to-purple-900/10 transition-all duration-300 transform hover:scale-[1.02]">
                                <td class="px-8 py-6">
                                    <div class="flex items-center">
                                        <div class="flex-shrink-0 w-12 h-12 bg-gradient-to-br from-blue-500 to-purple-600 rounded-2xl flex items-center justify-center shadow-lg group-hover:scale-110 transition-transform duration-300">
                                            <i class='bx bx-user text-xl text-white'></i>
                                        </div>
                                        <div class="ml-4">
                                            <div class="text-lg font-bold text-gray-900 dark:text-white group-hover:text-blue-600 dark:group-hover:text-blue-400 transition-colors">
                                                @Html.DisplayFor(modelItem => item.TenTG)
                                            </div>
                                        </div>
                                    </div>
                                </td>
                                <td class="px-8 py-6 whitespace-nowrap">
                                    @if (!string.IsNullOrEmpty(item.QuocTich))
                                    {
                                        <span class="inline-flex items-center px-4 py-2 rounded-full text-sm font-bold bg-blue-100 dark:bg-blue-900/30 text-blue-800 dark:text-blue-300 border border-blue-200 dark:border-blue-700">
                                            <i class='bx bx-flag mr-2'></i>
                                            @item.QuocTich
                                        </span>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center px-4 py-2 rounded-full text-sm font-medium bg-gray-100 dark:bg-gray-700 text-gray-600 dark:text-gray-400">
                                            <i class='bx bx-question-mark mr-2'></i>
                                            Chưa cập nhật
                                        </span>
                                    }
                                </td>
                                <td class="px-8 py-6">
                                    @if (!string.IsNullOrEmpty(item.TieuSu))
                                    {
                                        <div class="max-w-md">
                                            <p class="text-gray-700 dark:text-gray-300 line-clamp-2">@item.TieuSu</p>
                                        </div>
                                    }
                                    else
                                    {
                                        <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-medium bg-red-100 dark:bg-red-900/30 text-red-800 dark:text-red-300">
                                            <i class='bx bx-x-circle mr-1'></i>
                                            Chưa có tiểu sử
                                        </span>
                                    }
                                </td>
                                <td class="px-8 py-6 whitespace-nowrap">
                                    <div class="flex items-center gap-2 opacity-0 group-hover:opacity-100 transition-all duration-300 transform translate-x-4 group-hover:translate-x-0">
                                        <!-- Edit -->
                                        <a asp-action="SuaTacGia" asp-route-id="@item.MaTacGia"
                                           class="inline-flex items-center p-3 text-gray-400 hover:text-blue-600 hover:bg-blue-50 dark:hover:bg-blue-900/20 rounded-2xl transition-all duration-300 hover:scale-110 hover:shadow-lg group/action"
                                           title="Sửa thông tin">
                                            <i class='bx bx-edit text-xl group-hover/action:scale-110 transition-transform'></i>
                                        </a>

                                        <!-- Details -->
                                        <a asp-action="ChiTietTacGia" asp-route-id="@item.MaTacGia"
                                           class="inline-flex items-center p-3 text-gray-400 hover:text-green-600 hover:bg-green-50 dark:hover:bg-green-900/20 rounded-2xl transition-all duration-300 hover:scale-110 hover:shadow-lg group/action"
                                           title="Chi tiết">
                                            <i class='bx bx-show text-xl group-hover/action:scale-110 transition-transform'></i>
                                        </a>

                                        <!-- Delete -->
                                        <a asp-action="XoaTacGia" asp-route-id="@item.MaTacGia"
                                           class="inline-flex items-center p-3 text-gray-400 hover:text-red-600 hover:bg-red-50 dark:hover:bg-red-900/20 rounded-2xl transition-all duration-300 hover:scale-110 hover:shadow-lg group/action"
                                           title="Xóa"
                                           onclick="return confirm('Bạn có chắc muốn xóa tác giả \'@item.TenTG\'?');">
                                            <i class='bx bx-trash text-xl group-hover/action:scale-110 transition-transform'></i>
                                        </a>
                                    </div>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>

            <!-- Footer -->
            <div class="px-8 py-4 border-t-2 border-gray-200/30 dark:border-gray-700/30 bg-gradient-to-r from-gray-50 to-blue-50 dark:from-gray-800 dark:to-blue-900/10">
                <div class="flex flex-col sm:flex-row items-center justify-between">
                    <div class="text-sm text-gray-600 dark:text-gray-400 mb-4 sm:mb-0">
                        <i class='bx bx-info-circle mr-2'></i>
                        Hiển thị @Model.Count() tác giả • Cập nhật lúc: @DateTime.Now.ToString("HH:mm dd/MM/yyyy")
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    .line-clamp-2 {
        display: -webkit-box;
        -webkit-line-clamp: 2;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }

    /* Custom scrollbar */
    .overflow-x-auto::-webkit-scrollbar {
        height: 8px;
    }

    .overflow-x-auto::-webkit-scrollbar-track {
        background: #f1f5f9;
        border-radius: 10px;
    }

    .overflow-x-auto::-webkit-scrollbar-thumb {
        background: linear-gradient(135deg, #3b82f6, #8b5cf6);
        border-radius: 10px;
    }

        .overflow-x-auto::-webkit-scrollbar-thumb:hover {
            background: linear-gradient(135deg, #2563eb, #7c3aed);
        }
</style>

<script>
    // Hiển thị thông báo từ TempData
    document.addEventListener('DOMContentLoaded', function() {
        @if (TempData["Success"] != null)
        {
                <text>showNotification('@TempData["Success"]', 'success');</text>
        }
        @if (TempData["Error"] != null)
        {
                <text>showNotification('@TempData["Error"]', 'error');</text>
        }
    });

    function showNotification(message, type) {
        const notification = document.createElement('div');
        notification.className = `fixed top-4 right-4 p-4 rounded-2xl shadow-2xl z-50 transform transition-all duration-500 ${
            type === 'success'
                ? 'bg-gradient-to-r from-green-500 to-emerald-600 text-white'
                : 'bg-gradient-to-r from-red-500 to-rose-600 text-white'
        }`;

        notification.innerHTML = `
            <div class="flex items-center gap-3">
                <i class='bx ${type === 'success' ? 'bx-check-circle' : 'bx-error'} text-xl'></i>
                <span>${message}</span>
            </div>
        `;

        document.body.appendChild(notification);

        // Tự động ẩn sau 5 giây
        setTimeout(() => {
            notification.remove();
        }, 5000);
    }
</script>